# Translation Management

The domain module owns tenant-scoped translation jobs, source changes, vendor adapters, assignments, review decisions, translation memory, glossaries, cost provenance, and reconciliation. Provider integrations implement `TranslationVendorInterface` and are registered through `TranslationVendorRegistry`.
