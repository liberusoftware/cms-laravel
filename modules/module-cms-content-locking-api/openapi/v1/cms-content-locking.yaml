openapi: 3.1.0
info:
  title: CMS Content Locking API
  version: 1.0.0
paths:
  /api/v1/cms/content-locking/locks:
    post:
      operationId: acquireContentLock
      responses:
        '201': {description: Lock acquired}
