# Layout Builder

liberusoftware/module-cms-layout-builder provides the Layout Builder capability for Liberu CMS.

Validated, tenant-scoped layouts and regions for Liberu CMS.

This package is independently installable and exposes its public domain boundary through the CMS module system.
