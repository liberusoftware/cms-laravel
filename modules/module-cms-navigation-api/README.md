# CMS Navigation API

Optional API presentation package for the Navigation capability. It exposes
`/api/v1/cms/navigation` CRUD operations and nested item mutations, while all
menu, link-type, hierarchy, visibility, and tenant invariants remain in the
core `MenuService` boundary.
