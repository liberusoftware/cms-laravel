# Video and Audio Livewire

Provides the `module-cms-video-and-audio::media-browser` component with bounded search, kind filtering, pagination, and playback metadata loading through the domain query and service boundaries.
