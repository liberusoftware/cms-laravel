# cms-content-federation

liberusoftware/module-cms-content-federation provides the cms-content-federation capability for Liberu CMS.

CMS remote content sources, normalized references, health, and fallback cache.

This package is independently installable and exposes its public domain boundary through the CMS module system.
