# CMS Theme Integration

Tenant-aware site and channel theme selection with explicit fallback, region
component registration, view contracts, inheritance-compatible keys, and
preview tokens. The existing `cms-themes` manager remains intact and reusable.
