# Multisite API

Registers tenant-aware `/api/v1/cms/multisite` endpoints for site lifecycle, admins, quotas, cross-site references, and network transitions. Business rules remain in `MultisiteService`.
