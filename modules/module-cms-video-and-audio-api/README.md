# Video and Audio API

This optional presentation package exposes the versioned API registry endpoints for audiovisual assets. The OpenAPI 3.1 fragment is at `openapi/v1/cms-video-and-audio.yaml`; all mutations delegate to the domain `MediaManagementService`.
