# Structured Collections Livewire

Provides the module-cms-collections.collection-browser component with
tenant-scoped, searchable collection records while keeping queries and
publication rules in the Collections module.
