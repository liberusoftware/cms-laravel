# cms-content-locking

liberusoftware/module-cms-content-locking provides the cms-content-locking capability for Liberu CMS.

CMS optimistic locks, presence, conflict comparison, merge, and recovery.

This package is independently installable and exposes its public domain boundary through the CMS module system.
