# Structured Collections

liberusoftware/module-cms-structured-collections provides the Structured Collections capability for Liberu CMS.

Canonical compatibility package for CMS structured collections.

This package is independently installable and exposes its public domain boundary through the CMS module system.
