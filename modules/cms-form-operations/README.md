# CMS Form Operations

Provides the operational security boundary around form submissions: explicit
consent, tenant isolation, configurable rate limits, encrypted payloads,
retention timestamps, and authorized export/decryption through one service.
CRM and notification integrations can consume this boundary without reading
private storage directly.
