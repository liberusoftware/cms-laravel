# Extension Manager

liberusoftware/module-cms-extension-manager provides the Extension Manager capability for Liberu CMS.

Safe module lifecycle management for Liberu CMS extensions.

This package is independently installable and exposes its public domain boundary through the CMS module system.
