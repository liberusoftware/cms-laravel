# Static Publishing API

Exposes build creation and inspection, invalidation, rollback, diagnostics, and deployment operations through the application API registry. Route shape validation occurs at the HTTP boundary; build state and adapter rules remain in the domain service.
