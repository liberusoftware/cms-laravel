# CMS Personalization

The domain boundary for tenant-scoped audiences, context rules, variants, priority, fallback, consent, deterministic holdouts, and privacy-safe decision evidence. Use `PersonalizationService` for mutations and `DecisionEngine` for decisions.
