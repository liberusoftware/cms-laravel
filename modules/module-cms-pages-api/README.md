# CMS Pages API

Optional API presentation package for the Pages module. It exposes the
module-owned path, hierarchy, breadcrumb, alias, home/error, and template
read model under `/api/v1/cms/pages`.
