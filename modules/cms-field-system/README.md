# cms-field-system

liberusoftware/module-cms-field-system provides the cms-field-system capability for Liberu CMS.

CMS reusable field schemas, validation, computed and conditional fields, and schema versions.

This package is independently installable and exposes its public domain boundary through the CMS module system.
