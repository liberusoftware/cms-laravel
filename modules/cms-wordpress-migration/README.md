# WordPress Migration

liberusoftware/module-cms-wordpress-migration provides the WordPress Migration capability for Liberu CMS.

Provider-neutral WordPress migration runs, records, and source identifiers.

This package is independently installable and exposes its public domain boundary through the CMS module system.
