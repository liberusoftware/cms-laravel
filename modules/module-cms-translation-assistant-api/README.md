# Translation Assistant API

This optional API adapter exposes draft, checking, review, glossary, and style-rule operations through the application API registry. Validation and approval invariants remain in `TranslationAssistantService`.
