# Metadata

liberusoftware/module-cms-metadata provides the Metadata capability for Liberu CMS.

Generic tenant-scoped metadata for Liberu CMS entities.

This package is independently installable and exposes its public domain boundary through the CMS module system.
