# CMS Structured Collections API

This package exposes the canonical `/api/v1/cms/structured-collections` boundary for collections and records. It delegates persistence, tenancy, validation, and mutation invariants to the `cms-structured-collections` domain boundary. The OpenAPI fragment is at `openapi/v1/cms-structured-collections.yaml`.
