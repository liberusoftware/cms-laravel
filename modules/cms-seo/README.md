# SEO

The domain module owns tenant-scoped metadata persistence, canonical URL validation, robots defaults, structured/social metadata, and content quality scoring. HTTP and Livewire surfaces delegate to `SeoMetadataService`.
