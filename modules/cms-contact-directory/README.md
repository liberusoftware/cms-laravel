# cms-contact-directory

liberusoftware/module-cms-contact-directory provides the cms-contact-directory capability for Liberu CMS.

CMS contact directory domain module.

This package is independently installable and exposes its public domain boundary through the CMS module system.
