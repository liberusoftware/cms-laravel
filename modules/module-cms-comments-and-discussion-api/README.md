# Comments and Discussion API

Optional API adapter for `cms-comments-and-discussion`. It registers only the module's versioned comment, moderation, report, and subscription endpoints and delegates all domain behavior to the core package.
